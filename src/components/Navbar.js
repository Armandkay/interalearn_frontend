import React from 'react';
import './Navbar.css'; // Import the CSS file for styling

const Navbar = () => {
  return (
    <nav>
      <h1>InteraLearn</h1>
    </nav>
  );
};

export default Navbar;

